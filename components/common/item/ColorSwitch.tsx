import HSButton from "../input/HSButton";

export default function ColorSwitch() {
  return (
    <div style={{display: 'flex', gap: 10}}>
      {/*
      <HSButton onClick={() => setColorAndDark('#FF0000', true)} text={'red'}/>
      <HSButton onClick={() => setColorAndDark('#00FF00', true)} text={'green'}/>
      <HSButton onClick={() => setColorAndDark('#0000FF', true)} text={'blue'}/>
      <HSButton onClick={() => setColorAndDark('#FFF', false)} text={'white'}/>
      <HSButton onClick={() => setColorAndDark('#000', true)} text={'black'}/>
      */}
    </div>
  )
}